[package]
name = "squirrels"
version = "0.1.0"
edition = "2024"

[dependencies]
bstr = { version = "1.12.1", features = ["alloc", "std"], default-features = false }
squirrels-sys = { version = "0.1.0", path = "../squirrels-sys" }
thiserror = "2.0.18"
